services:
  - type: web
    name: dr-gonzaga-website
    env: php
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: php -S 0.0.0.0:$PORT
    envVars:
      - key: PHP_VERSION
        value: 8.1

